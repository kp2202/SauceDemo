{
  "name": "saucedemo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:staging": "cross-env BUILD_TYPE=staging playwright test",
    "test:prod": "cross-env BUILD_TYPE=production playwright test",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "@types/node": "^20.0.0",
    "cross-env": "^10.0.0",
    "dotenv": "^17.4.2",
    "prettier": "^3.9.6"
  }
}
  